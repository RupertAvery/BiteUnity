module MainModule;

import System;
using System;

import UnitySystem;
using UnitySystem;

while ( true ) {
    sync {
        var b = GameObjectFromUnity.transform.position;
    }

    b.x += 0.001;

    sync {
        GameObjectFromUnity.transform.position = b;
    }
}
